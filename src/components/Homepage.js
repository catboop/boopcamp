import React from "react";
import { hot } from "react-hot-loader";

function Homepage() {
  return (
    <nav>
      <div>Sign in</div>
      <div>Sign up</div>
    </nav>
  );
}

export default hot(module)(Homepage);
